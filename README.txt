Projeto gerado: address-manager (Spring Boot + H2)

Como rodar:
1. Extraia o zip.
2. No terminal, vá para a pasta do projeto.
   cd address-manager
3. Rode:
   mvn spring-boot:run
4. Endpoints:
   - API base: http://localhost:8080/api/addresses
   - H2 console: http://localhost:8080/h2-console
     JDBC URL: jdbc:h2:mem:testdb
     User: sa
     Password: password
